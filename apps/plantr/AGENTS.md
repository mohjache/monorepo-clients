# Plantr Agent Notes
Use the product and marketing specification here before making content, design, SEO, or conversion changes:

- [MARKETING_SITE_SPEC.md](C:/dev/personal/monorepo-clients/apps/plantr/specs/MARKETING_SITE_SPEC.md)

Implementation guidance:
- use shared `@repo/ui` components everywhere possible

Verifying changes:
- must run `pnpm check:unsafe` and everything must pass.


## Local setup

Run the app from the monorepo root with:
- `pnpm --filter plantr dev`
